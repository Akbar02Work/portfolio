# Technical Audit Report

## 1. ⚡️ Performance (Производительность)

1. **Файл/Компонент:** `vite.config.ts` / Bundle
2. **Проблема:** Весь код собирается в один огромный JS-файл (`index.js` ~340KB). Это замедляет первоначальную загрузку (FCP/LCP), так как браузер должен загрузить и распарсить всё сразу.
3. **Критичность:** 🔴 High
4. **Решение:** Настроить разделение чанков (Code Splitting) в Vite.
   ```typescript
   // vite.config.ts
   export default defineConfig({
     build: {
       rollupOptions: {
         output: {
           manualChunks: {
             vendor: ['react', 'react-dom', 'react-router-dom', 'framer-motion'],
             ui: ['@radix-ui/react-dialog', '@radix-ui/react-slot', 'lucide-react']
           }
         }
       }
     }
   });
   ```

2. **Файл/Компонент:** `src/App.tsx`
3. **Проблема:** Маршруты импортируются статически (`import Index from ...`). Это означает, что код всех страниц загружается сразу, даже если пользователь открывает только главную.
4. **Критичность:** 🟡 Medium
5. **Решение:** Использовать `React.lazy` и `Suspense`.
   ```tsx
   import { Suspense, lazy } from 'react';
   const Index = lazy(() => import('./pages/Index'));
   const ProjectDetail = lazy(() => import('./pages/ProjectDetail'));
   const NotFound = lazy(() => import('./pages/NotFound'));

   const App = () => (
     <Suspense fallback={<div>Loading...</div>}>
       <Routes>
         <Route path="/" element={<Index />} />
         ...
       </Routes>
     </Suspense>
   );
   ```

3. **Файл/Компонент:** `src/data/projects.ts`
4. **Проблема:** Изображения импортируются через JS (`import projectImg1 from ...`). Это заставляет Vite включать ссылки на них (или инлайнить их) в основной бандл, даже если они нужны только на детальной странице.
5. **Критичность:** 🟡 Medium
6. **Решение:** Перенести изображения в папку `public/images/` и использовать строковые пути.
   ```typescript
   // src/data/projects.ts
   export const projects: Project[] = [
     {
       image: "/images/project-1.png", // Ссылка на public
       ...
     }
   ];
   ```

## 2. 🛡️ Code Quality & Bugs (Баги и Надежность)

1. **Файл/Компонент:** `tsconfig.app.json`
2. **Проблема:** Отключен строгий режим (`"strict": false`, `"noImplicitAny": false`). Это сводит на нет преимущества TypeScript, допуская скрытые баги и `any` типы.
3. **Критичность:** 🔴 High
4. **Решение:** Включить строгий режим.
   ```json
   // tsconfig.app.json
   {
     "compilerOptions": {
       "strict": true,
       "noImplicitAny": true,
       "strictNullChecks": true
       ...
     }
   }
   ```

2. **Файл/Компонент:** `src/components/sections/Hero.tsx`
3. **Проблема:** Дублирование логики навигации. Массив `navLinks` используется для мобильного меню, но десктопное меню хардкодит ссылки (`<li><a href="#home">...</a></li>`). Если вы измените ссылку в массиве, десктопное меню не обновится.
4. **Критичность:** 🟡 Medium
5. **Решение:** Использовать `.map()` и для десктопного меню.
   ```tsx
   <ul className="font-medium flex flex-row space-x-8">
     {navLinks.map((link) => (
       <li key={link.href}>
         <a href={link.href} className="...">
           {link.label}
         </a>
       </li>
     ))}
   </ul>
   ```

3. **Файл/Компонент:** `src/components/ui/sonner.tsx`
4. **Проблема:** Lint warning: "Fast refresh only works when a file only exports components". Это может ломать Hot Module Replacement.
5. **Критичность:** 🟢 Low
6. **Решение:** Убедиться, что файл экспортирует только компоненты, или вынести константы/утилиты в отдельный файл.

## 3. 🧹 Cleanup & Structure (Чистота)

1. **Файл/Компонент:** `src/assets/`
2. **Проблема:** Найдены неиспользуемые файлы изображений.
   - `profile-placeholder.png` (Hero использует `/avatar.png` из public)
   - `project-placeholder-5.png`
   - `project-placeholder-6.png`
3. **Критичность:** 🟢 Low
4. **Решение:** Удалить лишние файлы.

2. **Файл/Компонент:** `src/components/sections/Projects.tsx`
3. **Проблема:** Жестко прописанные цвета и градиенты в массиве `projectStyles`. Лучше вынести их в конфигурацию или константы, чтобы отделить данные от представления.
4. **Критичность:** 🟢 Low
5. **Решение:** Определить стили в `src/constants/styles.ts` или расширить тему Tailwind.

## 4. 🚀 Optimization Suggestions (Предложения)

1. **SEO (React Helmet):**
   - Сейчас мета-теги статичны в `index.html`. При переходе на страницу проекта (`/projects/:slug`), заголовок и описание страницы не меняются, что плохо для SEO и шаринга в соцсетях.
   - **Решение:** Добавить `react-helmet-async` и обновлять `<title>` и `<meta>` в `ProjectDetail.tsx`.

2. **Image Optimization:**
   - Добавьте плагин `vite-plugin-image-optimizer` для автоматического сжатия изображений при билде.

---

### 🗑️ Файлы для удаления:
1. `src/assets/profile-placeholder.png`
2. `src/assets/project-placeholder-5.png`
3. `src/assets/project-placeholder-6.png`
